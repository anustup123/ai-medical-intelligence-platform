version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
      - ./predictions.db:/app/predictions.db
    env_file:
      - .env
    restart: unless-stopped
